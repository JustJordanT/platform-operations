name: Build

on: push

jobs:
  release:
    name: Build & Run
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Run Script
        run: |
          cd github-policy
          python main.py
        env:
          TOKEN: ${{ secrets.PYTHON_TOKEN }}